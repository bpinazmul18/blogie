// ActionTypes

// Actions

// Reducer

const initalState = {
  list: [],
  selectPosts: null,
  lastFetch: null,
}

const postsReducer = (posts = initalState, action) => {
  return posts
}

export default postsReducer
